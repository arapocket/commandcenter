include mixins
extends layout
block content   
			include navbar
			h3 Patrol History
			br
			div
				table#pickle.table.table-hover.table-striped.table-bordered
					thead
						tr.primary 
							th
							th Time
							th First Name
							th Last Name
					tbody
						-each result in results
							tr
									td
										a(href="/patrolreplay/#{result.PatrolID}")  View Replay
									td=result.Start
									td=result.End
									td=result.FirstName
									td=result.LastName
				script.
					$(document).ready(function() {
					$('#pickle').DataTable();
					});
				script.
					$(document).ready(function() {
					$('#pickle tr').click(function() {
					var href = $(this).find("a").attr("href");
						if(href) {
							window.location = href;
						}
					});
					});
		   